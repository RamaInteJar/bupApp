<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>gitPub - Drinks Menu</title>
    <style>
        body{
         background-color: #616D3D;
        
        }
        h1{
            text-align: center;
            font-size: 42px;
            color: #5F007C;
        }
        .listContainer{
            height: 500px;
            width: 700px;
            background-image: url("https://images.unsplash.com/photo-1437418747212-8d9709afab22?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=987&q=80");
            background-size: cover;
          margin: auto;
          font-size: 36px;
          border-radius: 12px;
            
        }

        li{
            margin: 6px;
            color: wheat; 
        }
            h2{
                font-size: 40px;
                color: wheat;
                text-align: center;
            }

            h3{
                text-align: center;
                font-size: 32px;
            }
            a{
                color: #F37413;
            }

    </style>
</head>
<body>
    <!-- Main Heading -->
    <h1>Allons boire gitPub</h1>
    <div class="listContainer">

        <!-- Sub Heading for Drinks Menu -->
        <h2>Drinks Menu</h2>
    
        <!-- Unordered List for Drinks -->
        <ul>
            <!-- Loop through each drink in the "drinks" array -->
            <% drinks.forEach((drink, index) => { %>
            <li>
                <!-- Convert drink name to Title Case -->
                <% let formattedDrinkArray = [] %>
                <% drink.name.split(' ').forEach(word => { 
                    newWord = word.charAt(0).toUpperCase() + word.slice(1)
                    formattedDrinkArray.push(newWord)
                }) %>
                
                <!-- Display drink name as a link with index as parameter -->
                <a href="/drinks/<%= index %>"><%= formattedDrinkArray.join(' ') %></a>
            </li>
            <% }) %>
        </ul>


    </div>

    <!-- Sub Heading with link to Food Menu -->
    <h3>Hungry? Find our Food Menu <a href="/food">here!</a></h3>
</body>
</html>
